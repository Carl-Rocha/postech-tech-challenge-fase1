import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // basePath do admin pra sair tudo em /admin (assets, rota etc)
  basePath: "/admin",
  reactStrictMode: true,
};

export default nextConfig;
